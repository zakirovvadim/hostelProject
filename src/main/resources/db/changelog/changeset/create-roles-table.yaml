databaseChangeLog:
  #  - preConditions:
  #      - tableExists:
  #          onFail: warn
  #          tableName: usr
  - changeSet:
      id: create-roles
      author: Vadim
      changes:
        - createTable:
            tableName: roles
            columns:
              - column:
                  name: id
                  type: bigint
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: name
                  type: varchar(255)