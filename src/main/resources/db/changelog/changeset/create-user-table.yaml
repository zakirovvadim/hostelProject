databaseChangeLog:
#  - preConditions:
#      - tableExists:
#          onFail: warn
#          tableName: usr
  - changeSet:
      id: create-usr
      author: Vadim
      changes:
        - createTable:
            tableName: usr
            columns:
              - column:
                  name: id
                  type: varchar(255)
                  # autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: name
                  type: varchar(255)
              - column:
                  name: email
                  type: varchar(255)